package threeSum;

import java.util.*;

class Solution {

    public List<List<Integer>> threeSum(int[] nums) {

        int[] sorted = Arrays.stream(nums).sorted().toArray();



        int firstCurrentIndex;

        int secondCurrentIndex;

        int key = 0 - firstCurrentIndex - secondCurrentIndex;

        if (firstCurrentIndex == 0) {
            Arrays.binarySearch(sorted, 1, sorted.length, )

        }

        Arrays.binarySearch()

        Set<int[]> resultSet = new HashSet<>();



        return null;
    }
}
